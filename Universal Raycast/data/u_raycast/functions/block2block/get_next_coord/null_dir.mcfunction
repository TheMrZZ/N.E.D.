# Add 2 blocks to avoid this coordinate to be taken as the closest coordinate

scoreboard players operation #next_coord u_raycast += factor u_raycast
scoreboard players operation #next_coord u_raycast += factor u_raycast